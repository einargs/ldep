# ldep
ldep is a small personal project that aims to create a simple dependently typed
language using the [FStar](https://www.fstar-lang.org/) language. Currently it
is in the very, very early stages.
